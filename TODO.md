


TODOS
1. fix filters in runs page -> filter by differnt paramters
2. fix filters in reports page -> (aggregated runs)
3. add update to Scenario Tasks  to genrate new tasks types (manual or using ai)
4. make the Website Analysis Summary component in scenario card modal to be more readable
5. make Persona Distribution component to be dynamic based o the generataed personas query using rest api
6. ask browsing model not to navigate but only use browser  events like clicks to navigate like as a user would do
7. fix theme colors for dark mode - colors are not looking good and the sankey diagrm as well
8. change the favicon in the tab and generate a logo with a niuce facivon
10. populate the journey_path tab in the persona run Run Details modal to show and it needs to be based on the events of the run
11. move background tasks of browser agent to run multithrdaded instead of background tasks so we could control how many parallel threads are running
12. imrpove code readbility, reuse, strucutre, and wirting of ui
13. imrpove code readbility, reuse, strucutre, and wirting of backend

Browser use
error in browser use run check if and how can take care of
1. ⚠️  WARNING: Total EventBus memory usage is 55.3MB (>50MB limit)
2. check why even if headless = true, it still opens a chromium borwser in the background that i see
